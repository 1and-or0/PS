ㅡㅡㅡㅡㅡ idea ㅡㅡㅡㅡㅡ
Enter를 기준으로 입력받는다
입력받은 이름의 집합을 구한다
개수를 정답에 더한다
반복한다
출력한다

ㅡㅡㅡㅡㅡ idea ㅡㅡㅡㅡㅡ
# 소수 m = 100003
# 해시 식 ai*r^i % m 


해시 함수를 만든다
bool visit[해시 테이블 크기]
문자열을 해시 함수를 통해 
숫자로 치환한다
위의 숫자를 visit의 인덱스로 사용한다
Enter를 기준으로 visit[위의 숫자]가 참인지 판단한다
거짓이면 첫 등장하는 이름 == 곰곰티콘 사용
참으로 바꿔주고 정답 저장 변수 +=1 
이미 참이면 그냥 스킵
Enter가 새로 들어오면 
bool visit를 다시 모두 거짓으로 초기화한다?
