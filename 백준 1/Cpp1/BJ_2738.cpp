#ifndef BITS_STDC_H2738
#define BITS_STDC_H2738
#include <bits/stdc++.h>
using namespace std;
#endif // !BITS_STDC_H2738

int main_()
{
	int n, m;
	cin >> n >> m;

	vector<int> test;

	vector<vector<int>> a;
	vector<vector<int>> b;

	test.push_back(1);
	test.push_back(2);
	test.push_back(3);
	
	deque<int> q;
	q.push_back(100);
	q.push_back(200);
	q.push_back(300);
	q.pop_front();

	
	return EXIT_SUCCESS;
}