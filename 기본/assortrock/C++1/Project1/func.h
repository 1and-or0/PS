#pragma once

// 반환타입 int이고 이름은 Add이며, 인자 두 개를 받는 함수가 있어요 라고 선언만 함. 함수 정의는 하지 않은(함수 내용은 작성하지 않음)
int Add(int a, int b);

int Sub(int a, int b);

int Mul(int a, int b);

int Div(int a, int b);

void printing();