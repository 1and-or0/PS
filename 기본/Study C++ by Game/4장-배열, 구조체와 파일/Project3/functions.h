#include <cstdio>
#include <cstring>

int findMaxValue(int src[], int len);

int findMaxChar(const char src[]);

void printArray(const int src[], int len, const char msg[] = "Array");

void reverseArray(const int src[], int dst[], int len);

void reverseString(const char src[], char dst[]);
