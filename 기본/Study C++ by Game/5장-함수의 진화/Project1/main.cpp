#include <cstdio>

int main()
{
	//int x = 10;
	//int* px = &x;
	//int** ppx = &px;

	//printf("변수 x 값: %d, *px: %d, **ppx: %d\n", x, *px, **ppx);
	//printf("변수 x의 주소(&x): %d, px: %d, *ppx: %d\n", &x, px, *ppx);
	//printf("포인터 px 주소(&px): %d, ppx: %d\n", &px, ppx);
	//printf("포인터 ppx 주소(&ppx): %d\n", &ppx);
	//
	//printf("변수 x의 주소(&x): 0x%x, px: %x, *ppx: %x\n", &x, px, *ppx);
	//printf("포인터 px 주소(&px): %x, ppx: %x\n", &px, ppx);
	//printf("포인터 ppx 주소(&ppx): %x\n", &ppx);

	//*px = 20; 
	//printf("변수 x 값: %d, *px: %d, **ppx: %d\n", x, *px, **ppx);

	//**ppx = 200;
	//printf("변수 x 값: %d, *px: %d, **ppx: %d\n", x, *px, **ppx);

	int A[5], * p;
	p = &A[2];
	
	
	





	return 0;
}